# Normalize line endings: LF in the repo and in working copies on all
# platforms. Keeps Prettier (endOfLine: lf) deterministic regardless of
# each machine's core.autocrlf setting.
* text=auto eol=lf

# Binary assets — never touch line endings.
*.png binary
*.jpg binary
*.gif binary
*.ico binary
